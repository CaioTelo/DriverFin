# Apenas Docker Compose local. Substitua a senha e mantenha as URLs da API alinhadas.
POSTGRES_USER=driverfin
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_DB=driverfin
# PostgreSQL: localhost:5433. Web: localhost:3000. API: localhost:3001.
